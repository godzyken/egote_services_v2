targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
        generate_for:
          - lib/**/*.dart

      go_router_builder:
        generate_for:
          - lib/config/routes/*.dart
          - lib/features/common/domain/failures/error_route.dart

      riverpod_generator:
        generate_for:
          - lib/**/*.dart
