targets:
  $default:
    builders:
      json_serializable:
        options:
          field_rename: snake
        generate_for:
          - lib/**/*.dart
          - "!lib/config/routes/*.dart"

      go_router_builder:
        enabled: false

      riverpod_generator:
        generate_for:
          - lib/**/*.dart
          - "!lib/config/routes/*.dart"
